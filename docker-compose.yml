version: '3.1'

services:

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    hostname: sqlserver
    container_name: sqlserver
    environment:
      SA_PASSWORD: "fijnif-kuVxit-jivma4"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"